{
  "name": "slack-todoist",
  "description": "Add todo items to your Inbox",
  "repository": "https://github.com/ryankanno/slack-todoist",
  "website": "https://github.com/ryankanno/slack-todoist",
  "keywords": ["slack", "command", "bot", "chat"],
  "env": {
    "SLACK_HOOK_URL": {
      "description": "Slack incoming WebHook URL",
      "required": true
    },
    "USERNAME": {
      "description": "Optional: Username to use when replying with the conversion result (default: todoist)",
      "required": false
    },
    "EMOJI": {
      "description": "Optional: Emoji icon to use when replying with the conversion result (default: )",
      "required": false
    },
    "SLACK_TOKEN": {
      "description": "Optional: Additional security step: Slack slash command token for verification that the request came from your Slack team",
      "required": false
    }
  }
}
